# Checklist

- Command descriptions match the current parser surface: `list`, `link`, `sync`, and `unlink`.
- Shareability enforcement rules match the current metadata behavior.
- `.agents/config.json` examples use the current JSON shape and supported source forms.
- Source resolution rules distinguish repo-relative paths from `package:<name>` sources.
- Windows notes mention the junction fallback rather than implying elevation is always required.
- README examples and the reference skill stay aligned.
