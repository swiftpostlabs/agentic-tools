{
  "skill_name": "ref-sp-js-deno",
  "evals": [
    {
      "id": 1,
      "prompt": "We have a mixed repo with Deno utilities and a Node app. Explain how to configure the Deno side without making the tools fight over the same files.",
      "expected_output": "Guidance that centers deno.json, defines clear ownership boundaries, and avoids whole-workspace Deno activation as the default.",
      "assertions": [
        "The response mentions deno.json as the Deno source of truth",
        "The response addresses hybrid boundary or file-ownership issues",
        "The response does not recommend whole-workspace Deno activation as the default"
      ]
    },
    {
      "id": 2,
      "prompt": "Design a Deno CLI that reads local files and calls an HTTP API. What permissions and command structure should it use?",
      "expected_output": "A recommendation that uses explicit permissions, clear Deno tasks or commands, and modern dependency conventions.",
      "assertions": [
        "The response mentions explicit filesystem or network permissions",
        "The response recommends task or command wiring through Deno tooling",
        "The response avoids using -A as the default recommendation"
      ]
    },
    {
      "id": 3,
      "prompt": "We have an existing Node plus TypeScript repo with tsconfig.json and eslint.config.js, but we want Deno to own a utilities folder and use deno check and deno lint there. How should we set that up?",
      "expected_output": "Guidance that keeps deno.json as the Deno source of truth, scopes tsconfig or ESLint ownership carefully, mentions nodeModulesDir auto when ESLint or editor tooling needs local packages, and keeps Deno activation path-based.",
      "assertions": [
        "The response mentions deno.json as the Deno configuration owner",
        "The response explains when tsconfig.json can stay in the repo",
        "The response mentions nodeModulesDir or a local node_modules requirement for ESLint or editor tooling",
        "The response avoids recommending whole-workspace Deno activation"
      ]
    },
    {
      "id": 4,
      "prompt": "Install and set up an npm-backed CLI as a dev dependency in a Deno-owned repo, then add a task so contributors can run it.",
      "expected_output": "Guidance that installs the CLI with Deno's dependency manager, records the manifest and lockfile changes, and exposes the installed binary through a same-named deno task.",
      "assertions": [
        "The response recommends deno add --dev npm:<package> or an equivalent concrete package-specific command",
        "The response says to check in deno.json and lockfile changes",
        "The response adds a deno task that invokes the installed binary by name",
        "The response does not replace installation with a deno run npm:<package> task alias"
      ]
    }
  ]
}
